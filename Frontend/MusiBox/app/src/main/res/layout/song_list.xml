<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="8dp"
    android:background="#E0F2F1"
    android:gravity="center_vertical"> <!-- Align all child elements vertically in the center -->

    <!-- Album Art -->
    <ImageView
        android:id="@+id/albumArt"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:src="@drawable/cover"
        android:layout_marginEnd="8dp"
        android:contentDescription="Album Art"/>

    <!-- Song Info Section -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:gravity="center_vertical"> <!-- Vertically align the text -->

        <!-- Song Title -->
        <TextView
            android:id="@+id/songTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Song Title"
            android:textStyle="bold"
            android:textColor="#000" />

        <!-- Artist Name -->
        <TextView
            android:id="@+id/artist_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Artist Name"
            android:textColor="#666"
            android:textSize="12sp" />
    </LinearLayout>

    <!-- Options Icon -->
    <ImageView
        android:id="@+id/optionsIcon"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:src="@drawable/angle_small_down"
        android:contentDescription="@string/options"
        android:layout_gravity="center_vertical"
        android:padding="4dp" />
</LinearLayout>